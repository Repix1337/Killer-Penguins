rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /leaderboard/{document} {
      allow read: if true;  // Anyone can read leaderboard entries
      // Remove direct write access completely
      allow write: if false; 
    }
  }
}